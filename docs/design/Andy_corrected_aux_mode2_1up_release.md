# Andy — Corrected Player-Aux Conversion + Mode-2 1UP-Only HUD Release

**Agent:** Andy · **Accepted baseline:** Build 0283 (`d421e8c6…`) · **Rejected/preserved:** Build 0284
(`99942d04…`). Findings labelled **PROVEN / HYPOTHESIS / DISPROVEN**.

## BASELINE / BUILD0284 REJECTION
- **PROVEN:** Build 0284 remains REJECTED and preserved unchanged (SHA `99942d043d81a65970a238d3ec7cdf67760423ddef7797f2a6ac24d8171a65dd`).
  Rejection reason: its damage/reaction auxiliary table operand was generated by **blind arithmetic**
  (arcade 0x05DA5E + 0x200 = **0x05DC5E**) instead of the shift-aware map, pointing at unrelated data. Ordinary
  gameplay did not activate the damage/reaction state, so gameplay success did not validate that operand.
- All numbered ROMs 0280–0284 are preserved on disk.

## EFFECTIVE HUD BUILD CONFIGURATION
- **PROVEN:** built with `RASTAN_GAMEPLAY_HUD_SPRITES=2`. The generated config
  `apps/rastan-direct/out/pc090oj_config.inc` contains `.equ RASTAN_GAMEPLAY_HUD_SPRITES, 2`
  (not assumed — read back from the generated file). Mode 2 = **P1 / 1UP SCORE ONLY** gameplay HUD. Mode 0
  (used for 0283/0284) suppressed the score — that was configuration, **not** a renderer defect.
- Mode-2 adds the 1UP-score code, so the canonical coverage invariant moved from the Mode-0 value 0x184834 to
  the Mode-2 value **0x184A88** (+0x254). This is a coverage-constant update only; it does not change ROM bytes.

## CORRECTED AUX TABLE MAPPING
- **PROVEN:** the spec declares the operand shift-aware, not arithmetic — `shift_replacements[36]` (arcade
  0x054810): `operand_arcade_target: 0x05DA5E`, `operand_kind: abs_l_32bit`, `relocate_after_shift: true`,
  `replacement_template: 207C` (movea.l #imm,a0).
- After rebuild the generated instruction resolves to the **address-map-authoritative** target:
  `address_map.json`: arcade **0x05DA5E** ∈ arcade_copy seg [0x05A55A..0x060000], genesis_start 0x05A64E →
  runtime **0x05DB52**. Postpatched disasm: `5492a: 207c 0005 db52  moveal #383826,%a0` (count 1).
- **PROVEN:** the rejected operand **0x05DC5E is ABSENT** (count 0 in the postpatched disasm).

## CORRECTED PLAYER-AUX SOURCE
- **PROVEN:** original semantic family = player damage/reaction auxiliary; final constructor arcade 0x054810;
  duplicate raw HW producer arcade 0x052AA2 (deletion window arcade 0x052A64, `shift_replacements[3]`,
  `replacement_bytes: ""`).
- Postpatched 0x054810 producer (runtime 0x5492A): `moveal #0x5DB52,a0 ; muluw #24,d0 ; addaw d0,a0 ;
  jsr 0x72E1A (genesistan_pc090oj_hook_player_aux_native_54810) ; rts` — **no 0xD00000 write**.
- Native hook body: `move.w #NATIVE_LANE_FRONT_EFFECT, native_sprite_lane ; bsr native_sprite_emit ; rts` ⇒
  emits directly to **NATIVE_LANE_FRONT_EFFECT**.
- **PROVEN absent for this family:** duplicate raw `movea.l #0x00D00000` tail (count 0); the 12 remaining
  0x00D00000 references in the ROM are all in the frontend/general-PC090OJ infrastructure (0x3AF…, 0x4A908,
  0x512CE, 0x70000+ hook helpers) — **none** in the player-aux producer (0x549xx) or the deleted duplicate
  (0x52Axx). Virtual PC090OJ records 44..47 for this family are retired.
- **PROVEN preserved:** Build 0280 A5+0x1338 correction intact (`0x00FF1338` ×4 in the ROM); Build 0281 sword
  RIGHT-facing flip intact (inline code-index in d7 — `addaw %d7,%a2` ×4). No Block-A dependency restored.

## STATUS PRODUCER PRESERVATION
- **PROVEN:** `genesistan_pc090oj_hook_status_sprite_5a098` (Build 0283 direct-native energy/status producer)
  remains in source. Mode 2 intentionally does NOT display the status/energy row; the producer was NOT modified
  to force it visible. No object-RAM/frontend dependency reintroduced for it.

## MODE-2 1UP SCORE VALIDATION
- **PROVEN (static):** Mode 2 enables `.Lpc090oj_mode2_project_p1_hud` / `.Lnq_project_p1_hud`, which emits the
  live P1 score digits (BCD 0x00FF011E) into the HUD lane. The flag is verified = 2 (above). This is the SAME
  established Mode-2 P1-score implementation shipped in the prior accepted Mode-2 builds (0280/0281) — no score
  renderer redesign, no PC090OJ score records, no hardware record bands.
- **PROVEN (build smoke):** the Makefile-owned 30s Genesis-NTSC smoke ran during `make release`
  (`Average speed: 1004.53%`, no crash), confirming boot + non-interactive run.
- **NOT COMPLETED THIS SESSION (tooling):** the driven interactive Mode-2 run (score increment/expansion, digit
  update, no-stale-digits) could not be executed — every manual `mame genesis` invocation in this session
  produced no output (a controlled-MAME environment failure; the build's own make-invoked smoke works, direct
  invocations do not). The score's visual presence/behaviour is therefore deferred to Tighe's acceptance
  (USER MUST VERIFY). Static + established-implementation evidence supports YES.

## GAMEPLAY REGRESSION
- **PROVEN (architecture):** direct-native gameplay preserved — Build 0281 sword flip (d7 index), Build 0280
  A5+0x1338, Build 0282 collision-map, and the corrected player-aux conversion are all present in the built ROM.
  0 gameplay frontend-object-scanner / generic PC090OJ decoder execution by architecture (gameplay finalizer
  `.Lnq_gameplay` emits native lanes only).
- **PROVEN (build smoke):** 30s Genesis-NTSC smoke clean, no crash.
- **NOT COMPLETED THIS SESSION (tooling):** the driven Stage-1 regression (walk/jump/landing/standing+crouch
  attack, grounded Lizardman logical Y=121 / visible bottom=129) could not be executed due to the same
  controlled-MAME environment failure. The Lizardman Y=121/129 invariant is preserved by construction (Build
  0282 collision-map + no BODY change here) but not re-measured this session. Deferred to Tighe's acceptance.

## DOCUMENTATION UPDATES
- CLOSED_ISSUES / KNOWN_FINDINGS / GRAPHICS_STATUS / OPEN_ISSUES reconciled: 0284 rejected (arithmetic aux
  operand); corrected player-aux conversion released; Mode-0 score suppression was configuration not a defect;
  new candidate uses RASTAN_GAMEPLAY_HUD_SPRITES=2 for 1UP-only; status producer 0x5A098 stays direct-native;
  remaining PC090OJ compatibility families remain open.

## REMAINING PC090OJ DEBT
- Frontend/general PC090OJ object-RAM path still present for non-gameplay scenes (by design). Remaining
  PC090OJ producer families (other enemies/effects) not yet converted — OPEN, not in scope here.
