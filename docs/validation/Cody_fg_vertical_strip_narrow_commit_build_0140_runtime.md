# Cody - Build 0140 FG Narrow-Commit Runtime Validation

**Date:** 2026-07-05  
**Type:** Runtime evidence only  
**Build under test:** Build 0140  
**ROM:** `dist/rastan-direct/rastan_direct_video_test_build_0140.bin`  
**ROM SHA256:** `f6e63eb3e3a6d5e82caf9e151ef2eb1c23418633ee7118adad51f1c2081a135c`  
**Evidence:** `states/traces/fg_vertical_strip_narrow_commit_build_0140_autonomous_20260705_154323/`  
**Outcome:** **A - Runtime validation complete**

## 1. Executive Outcome

Attempt 1 autonomously reached the selected FG vertical-strip producer and captured a fully valid narrow-eligible invocation plus the following VBlank presentation. Native debugger evidence proves:

- selected wrapper `runtime_genesis_pc 0x055B90` hit `64` times;
- exactly `1` valid narrow invocation appended a descriptor;
- `63` invocations range-rejected before staging;
- `64` selected staging stores executed;
- selected broad `fg_row_dirty` writes executed `0` times;
- narrow presenter consumed count `1`, emitted `4` row runs x `16` words = `64` Plane-A words;
- VDP autoincrement was set for the narrow transfer and restored before the unchanged broad FG commit;
- descriptor count was cleared after consumption;
- no exception entry was observed.

This is frontend/attract-path validation only. It is **not** gameplay, Stage 1, or active gameplay validation.

## 2. Authorization And Autonomous Method

The task explicitly authorized autonomous MAME launch/control/termination. No user input was requested or required.

The capture used MAME's Genesis driver with Qt native debugger breakpoints and a Lua harness for controller input plus frame-limit termination. Native debugger evidence is treated as authoritative for instruction-owned stores and VDP writes. Lua write taps are retained as supplementary evidence and are known to under-report this path.

## 3. ROM Verification

`rom_sha256.txt` records:

```text
f6e63eb3e3a6d5e82caf9e151ef2eb1c23418633ee7118adad51f1c2081a135c  dist/rastan-direct/rastan_direct_video_test_build_0140.bin
```

The SHA matches the required Build 0140 SHA.

## 4. Execution-State Limitation

Build 0140 does not reach playable Genesis gameplay. This validation covers the currently reachable frontend/attract path. User-provided BlastEm visual evidence independently reports that Build 0140 reaches item-description, title, story, and high-score screens; this task did not ask for or require another user visual test.

## 5. Attempts Performed

Only Attempt 1 was needed. Attempts 2 and 3 were not run because Attempt 1 produced Outcome A evidence.

Primary clean Attempt 1:

```bash
MAX_FRAME=1700 timeout 300s states/traces/fg_vertical_strip_narrow_commit_build_0140_autonomous_20260705_154323/run_attempt.sh 1
```

Internal MAME command generated by `run_attempt.sh`:

```bash
/usr/games/mame genesis -cart /home/tighe/projects/rastan-genesis/dist/rastan-direct/rastan_direct_video_test_build_0140.bin -homepath states/traces/fg_vertical_strip_narrow_commit_build_0140_autonomous_20260705_154323/home -input_directory states/traces/fg_vertical_strip_narrow_commit_build_0140_autonomous_20260705_154323/input -snapshot_directory states/traces/fg_vertical_strip_narrow_commit_build_0140_autonomous_20260705_154323/snap -window -keyboardprovider auto -skip_gameinfo -debug -debugger qt -debuglog -autoboot_script states/traces/fg_vertical_strip_narrow_commit_build_0140_autonomous_20260705_154323/autonomous_input_and_taps.lua -debugscript states/traces/fg_vertical_strip_narrow_commit_build_0140_autonomous_20260705_154323/build0140_native_debug_attempt_1.cmd
```

A first Attempt 1 run was terminated by the host timeout after already capturing the same narrow evidence; those files are preserved with `_timeout` suffixes. The primary evidence used here is the clean rerun, which exited via Lua frame limit at frame `1700` with process exit code `0`.

## 6. Autonomous Input Timeline

Attempt 1 used the prior manual-style frontend sequence:

```text
FRAME 740 a DOWN
FRAME 761 a UP
FRAME 960 start DOWN
FRAME 1041 start UP
FRAME 1120 a DOWN
FRAME 1131 a UP
FRAME 1160 b DOWN
FRAME 1171 b UP
FRAME 1200 c DOWN
FRAME 1211 c UP
FRAME 1240 a DOWN
FRAME 1251 a UP
FRAME 1280 b DOWN
FRAME 1291 b UP
FRAME 1320 c DOWN
FRAME 1331 c UP
```

The Lua harness found `P1 Start`, `P1 A`, `P1 B`, and `P1 C` fields successfully.

## 7. Selected Producer Reach Result

Native debugger event counts:

| Event | Count |
|---|---:|
| `SELECTED_ENTRY_055B90` | `64` |
| `SELECTED_HELPER_ENTRY_0703EA` | `64` |
| `SELECTED_RETURN_055B96` | `64` |
| `SELECTED_RANGE_REJECT_0705EE` | `63` |

## 8. Invocation Classification

| Classification | Count | Evidence |
|---|---:|---|
| Fully valid narrow-eligible invocation | `1` | `DESC_COUNT_STORE_0705C6` |
| Destination/range rejected | `63` | `SELECTED_RANGE_REJECT_0705EE` |
| Per-descriptor invalid fallback | `0` | `SELECTED_INVALID_DESC_070568` absent |
| Wrap/unsupported fallback | `0` | wrap check passed: `col_x2=0`, `col_x2+120=0x78 < 0x80` |
| Capacity overflow fallback | `0` | count before append `0`, cap `64` |
| Broad fallback after staging | `0` | `SELECTED_BROAD_FALLBACK_0705CE` absent |

Wrap eligibility proof:

```text
EVENT SELECTED_WRAP_CHECK_BRANCH_07059C cyc=140925273 pc=07059E sr=2709 d0=00000078 col_plus_120=0078 col_x2=0000 count=0000
```

`col_offset_x2 + 120 = 0x78`, which is less than `0x80`, so the strict eligibility rule is satisfied.

## 9. Staging Evidence

Native debugger captured `64` selected stores at `runtime_genesis_pc 0x07054E`.

First four selected staging stores:

```text
EVENT SELECTED_STAGING_STORE_07054E ... eff=00FF509E ... d3=000160D8 ...
EVENT SELECTED_STAGING_STORE_07054E ... eff=00FF511E ... d3=000160DC ...
EVENT SELECTED_STAGING_STORE_07054E ... eff=00FF519E ... d3=000160E0 ...
EVENT SELECTED_STAGING_STORE_07054E ... eff=00FF521E ... d3=000160E4 ...
```

The first word of each of the four rows matches prior Build 0138 selected-frame evidence: `0x60D8`, `0x60DC`, `0x60E0`, `0x60E4`. The exact frame/state is not claimed byte-identical beyond this selected invocation comparison.

## 10. Descriptor Transaction Evidence

Descriptor append was content-before-count:

```text
EVENT DESC_BASE_STORE_0705B6 cyc=140925341 pc=0705B8 sr=2704 a0=00FF400A d3=00030000 base_row=0000 col_x2=0000 count_before=0000 pending=0000000F fg_dirty=00000000
EVENT DESC_COL_STORE_0705C0 cyc=140925373 pc=0705C2 sr=2704 a0=00FF400A d3=00030000 base_row=0000 col_x2=0000 count_before=0000 table_word=0000
EVENT DESC_COUNT_STORE_0705C6 cyc=140925389 pc=0705C8 sr=2700 d0=00000001 count_before=0000 table_word=0000 fg_dirty=00000000
```

Measurements:

- `base_row = 0x0000`
- `col_offset_x2 = 0x0000`
- descriptor count before append: `0`
- descriptor count after append: `1`
- descriptor-byte stores: `2`

## 11. Dirty Ownership Evidence

Selected producer broad dirty writes: `0`.

The descriptor append occurred with `fg_dirty=00000000`, and the following VBlank narrow presenter entered with `fg_dirty=00000000`. The selected valid path therefore suppressed broad dirty-row ownership as designed.

Fallback dirty path did not execute for the valid invocation:

- `SELECTED_BROAD_FALLBACK_0705CE`: `0`
- `SELECTED_FG_DIRTY_STORE_0705DA`: `0`

## 12. Narrow VDP Transfer Evidence

Narrow presenter entered with one descriptor:

```text
EVENT NARROW_ENTRY_071708 cyc=145553289 pc=07170A sr=2604 count=0001 fg_dirty=00000000 desc0=0000 s0=0002 s2=0002 s4=0005
```

Row table from `narrow_rows_attempt_1.tsv`:

| Row run | VRAM start | Source start | Words | First word | Last source | Last word |
|---:|---:|---:|---:|---:|---:|---:|
| 0 | `0xE000` | `0x00FF509E` | 16 | `0x60D8` | `0x00FF5116` | `0x608A` |
| 1 | `0xE080` | `0x00FF511E` | 16 | `0x60DC` | `0x00FF5196` | `0x608D` |
| 2 | `0xE100` | `0x00FF519E` | 16 | `0x60E0` | `0x00FF5216` | `0x6090` |
| 3 | `0xE180` | `0x00FF521E` | 16 | `0x60E4` | `0x00FF5296` | `0x6093` |

Measured totals:

- row runs: `4`
- words per run: `[16, 16, 16, 16]`
- total narrow Plane-A words: `64`
- source stride samples: all `8` bytes
- destination stride: `8` bytes by VDP autoincrement `0x08`

## 13. Autoincrement Evidence

```text
EVENT NARROW_AUTOINC_SET_071716 cyc=145553445 pc=071718 sr=2604 count=0001 d0=00000000 d1=4F800003 fg_dirty=00000000
EVENT NARROW_AUTOINC_RESTORE_071768 cyc=145557383 pc=07176A sr=2600 count=0001 fg_dirty=00000000
```

Interpretation:

- set: VDP register `0x0F = 0x08` before narrow transfer;
- restore: VDP register `0x0F = 0x02` after narrow transfer and before broad FG commit.

## 14. Descriptor Consumption

```text
EVENT NARROW_COUNT_CLEAR_071770 cyc=145557479 pc=071772 sr=2608 count_before=0001 fg_dirty=00000000
```

Descriptor count at VBlank entry was `1`; after consumption it returned to `0`.

## 15. Broad FG Interaction

After narrow consumption, the unchanged broad FG commit was entered with no FG dirty rows:

```text
EVENT FG_BROAD_COMMIT_ENTRY_070186 cyc=145557641 pc=070188 sr=2604 fg_dirty=00000000 narrow_count=0000 s0=0002 s2=0002 s4=0005
```

Broad FG rows after the narrow presenter: `0`.

This proves the selected narrow operation did not also trigger a selected-only 256-word broad row commit.

## 16. DISPLAY_OFF-To-DISPLAY_ON Measurement

The narrow transfer occurred inside the existing DISPLAY_OFF window:

```text
EVENT DISPLAY_OFF_SET_0700CE cyc=145481101 pc=0700D0 sr=2600 fg_dirty=00000000 narrow_count=0001 s0=0002 s2=0002 s4=0005
EVENT DISPLAY_ON_SET_0700E6 cyc=145579595 pc=0700E8 sr=2614 fg_dirty=00000000 narrow_count=0000 s0=0002 s2=0002 s4=0005
```

DISPLAY_OFF-to-DISPLAY_ON interval for the measured window: `98494` native cycles.

## 17. Exception Status

Exception events observed: `0`.

No address-error/crash-handler entry was observed in the validation capture.

## 18. Frontend / Attract State Reached

The measured narrow transfer occurred with frontend/attract state:

```text
s0=0002 s2=0002 s4=0005
```

The clean capture then continued to frame `1700` and final recorded frame state:

```text
FRAME frame=1700 seq=68 pc=071BA2 fg_dirty=00000000 narrow_count=0000 selected_stores=0 narrow_vdp_writes=0 broad_fg_vdp_writes=0 raw_fg_writes=0 s0=0002 s2=0003 s4=0000
```

Final state was `2/3/0`.

## 19. Comparison With Prior Build 0138 Evidence

Prior Build 0138 selected-frame evidence recorded first committed row words `0x60D8`, `0x60DC`, `0x60E0`, `0x60E4` for rows 0..3. Build 0140 native evidence shows the same first words for the selected narrow transfer rows.

Comparable facts:

| Fact | Build 0138 prior | Build 0140 measured |
|---|---:|---:|
| Selected wrapper entries | 64 | 64 |
| Valid selected invocation | 1 | 1 |
| Range rejects | 63 | 63 |
| Selected staged cells | 64 | 64 |
| First row words | `60D8/60DC/60E0/60E4` | `60D8/60DC/60E0/60E4` |
| Descriptor geometry | N/A in 0138 | `base_row=0`, `col_offset_x2=0` |
| Broad selected presentation | 256 Plane-A words | 0 selected broad words |
| Narrow selected presentation | N/A | 64 Plane-A words |

## 20. Limitations

- Validation is frontend/attract only, not gameplay or Stage 1.
- The Lua write taps under-report selected stores and VDP writes (`selected_stores=0`, `narrow_vdp_writes=0` in Lua metadata). Native debugger evidence is authoritative for this report.
- The first Attempt 1 run hit a host timeout after capturing equivalent evidence; it is preserved with `_timeout` suffixes but is not the primary run.
- Attempts 2 and 3 were not run because Attempt 1 completed Outcome A.

## 21. Outcome

**Outcome A: Runtime validation complete.**

Required proofs satisfied:

- descriptor append: YES;
- selected dirty suppression: YES;
- four 16-word strided runs: YES;
- 64 total narrow words: YES;
- autoincrement restoration: YES;
- descriptor count consumption: YES;
- no new exception: YES.

## 22. Evidence Index

Primary clean-run files:

- `build0140_native_debug_attempt_1.cmd`
- `native_debug_trace_attempt_1.log`
- `native_events_attempt_1.log`
- `native_event_counts_attempt_1.tsv`
- `selected_transaction_attempt_1.tsv`
- `narrow_rows_attempt_1.tsv`
- `reduced_summary_attempt_1.json`
- `reduced_summary_attempt_1.md`
- `autonomous_input_and_taps.lua`
- `input_timeline_attempt_1.log`
- `frame_markers_attempt_1.log`
- `mame_lua_metadata_attempt_1.log`
- `mame_stdout_attempt_1.log`
- `mame_stderr_attempt_1.log`
- `rom_sha256.txt`
- `evidence_index.tsv`

Timeout-suffixed files are retained for audit history but are not the primary evidence.

## Non-Actions

No source, specs, tools, Makefiles, ROM artifacts, build outputs, address mapping, WRAM layout, VBlank order, DISPLAY_OFF/ON placement, frame timing, input/gameplay logic, or frame pipeline were modified by this validation task.

## Open / Closed Issues Impact

OPEN-001 / PC080SN optimization context only. No issues opened or closed.

## KNOWN_FINDINGS Impact

Option A - no `KNOWN_FINDINGS.md` update. This validates Build 0140's selected producer-local narrow commit at runtime but does not introduce a new durable architectural finding.

## STOP Status

STOP triggered: NO.
