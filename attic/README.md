# Attic

This directory keeps preserved experiments that still contain useful work or
reference value, but are not the current baseline for the project.

Current attic entry:

- [startup-common-rom](/home/tighe/projects/rastan-genesis/attic/startup-common-rom)

Nothing in this directory should be treated as the primary build target unless
it is explicitly promoted back into `apps/`.
