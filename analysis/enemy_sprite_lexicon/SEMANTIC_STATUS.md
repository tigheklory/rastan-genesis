# Enemy/Sprite Lexicon — SEMANTIC CLOSURE IN PROGRESS (NOT PRODUCTION INPUT)

Neither artifact set is final semantic truth:
- `cody_provisional_*` = Cody's provisional graphics-signature clustering (52 rows) — KNOWN over-inflated.
- `INVALID_pass2_andy/` = Andy pass-2 outputs — INVALID: representatives were fabricated from
  `base_code + 3 consecutive PC090OJ codes` (not the proven compositor); canonicalized before category
  proof / dynamic +0x3E rerun / palette-color decode. DO NOT CONSUME.
- `corrected_semantic_families.json` = Andy static +0x3E evidence (families/base codes/palette nibble) —
  EVIDENCE, not a closed lexicon; category per selector not yet individually proven.

`index.html` / `families.json` / `contact_sheet.png` currently = Cody provisional baseline (labeled here).
Final canonical replacement occurs only after all semantic gates pass (see report closure flags).
